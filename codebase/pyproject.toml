[project]
name = "skynet-core"
version = "0.1.0"
description = "Core domain and pipeline contracts for the Skynet meeting assistant."
requires-python = ">=3.11"

dependencies = [
    "pydantic>=2.7,<3",
    "httpx>=0.27,<1",
    "websockets>=15,<17",
]